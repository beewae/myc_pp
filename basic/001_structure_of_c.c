/*
 * All codes are taken from *C primer Plus*
 * 2022.12.11, beewae
 */

#include <stdio.h>
int main(void)  /* 一个简单的C程序 */
{
  int num;  /* 定义一个名为 num 的变量 */
  num = 1;  /* 为 num 赋一个值 */
  printf("I am a simple ");  /* 使用 printf() 函数 */
  printf("computer.\n");
  printf("My favorite number is %d because it is first.\n", num);
  return 0;
}

// 1. preprocessor, Macro, #include, system header, user header
// 2. function, return value, name, main, parameter, function body, return value, function call, assignment
// 3. declaration, definition, initialize, definition position
// 4. token, name
// 5. comments
